import chatConstants from './constants'

const initState = {
}

export const chatReducer = (state = initState, action) =>  {
    switch (action.type) {
        default:
            return state
    }
}