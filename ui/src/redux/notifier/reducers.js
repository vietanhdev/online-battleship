import notifierConstants from './constants'

const initState = {}

export const notifierReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state
    }
}