// import messageConstants from './constants'

const initState = {
}

export const messageReducer = (state = initState, action) =>  {
    switch (action.type) {
        default:
            return state
    }
}